---
phase: 01-fix-images-encoding-brand-color-align-to-stitch-designs
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [IMG-01, IMG-02, TXT-01, TXT-02, COLOR-01, DESIGN-01]
files_modified:
  - dona-pattal-webApp/public/images/hero/paper-plate-stack.png
  - dona-pattal-webApp/public/images/products/product-dona.jpg
  - dona-pattal-webApp/public/images/products/product-plate.jpg
  - dona-pattal-webApp/public/images/editorial/leaf-dona-pattal.jpg
  - dona-pattal-webApp/public/images/editorial/catering-service.jpg
  - dona-pattal-webApp/public/images/editorial/sweet-shop-trays.jpg
  - dona-pattal-webApp/next.config.ts
  - dona-pattal-webApp/src/components/ui/EditorialImage.tsx
  - dona-pattal-webApp/src/components/ui/EditorialImage.test.tsx
  - dona-pattal-webApp/src/components/sections/HeroSection.tsx
  - dona-pattal-webApp/src/components/sections/HeroSection.test.tsx
  - dona-pattal-webApp/src/components/sections/MaterialMakingSection.tsx
  - dona-pattal-webApp/src/components/sections/MirzapurSection.tsx
  - dona-pattal-webApp/src/components/sections/products/FactoryDirectBanner.tsx
  - dona-pattal-webApp/src/components/sections/about/AboutHero.tsx
  - dona-pattal-webApp/src/components/sections/about/ProductFocusSection.tsx
  - dona-pattal-webApp/src/components/sections/contact/ContactHero.tsx
  - dona-pattal-webApp/src/app/wholesale/page.tsx

must_haves:
  truths:
    - "Every <Image> on /, /about, /contact, /wholesale, /products resolves to a locally hosted file under public/images/ and returns HTTP 200"
    - "No image on any route renders as an empty bordered box"
    - "No image alt text or caption claims to depict a factory interior or manufacturing workshop unless a real photo of one is supplied"
    - "EditorialImage renders at its intended aspect ratio (the Tailwind class is statically present in the source, not built from a template string)"
    - "next.config.ts contains no remote image host that the app does not use"
  artifacts:
    - path: "dona-pattal-webApp/public/images/hero/paper-plate-stack.png"
      provides: "Homepage hero photography (stacked printed paper plates, RGBA 1280x705)"
    - path: "dona-pattal-webApp/public/images/products/product-dona.jpg"
      provides: "Product card image for dona SKUs (already referenced by src/content/products.ts)"
    - path: "dona-pattal-webApp/public/images/products/product-plate.jpg"
      provides: "Product card image for plate SKUs (already referenced by src/content/products.ts)"
    - path: "dona-pattal-webApp/public/images/editorial/leaf-dona-pattal.jpg"
      provides: "About-page heritage photography (traditional sal-leaf dona and pattal)"
    - path: "dona-pattal-webApp/public/images/editorial/catering-service.jpg"
      provides: "Contact/wholesale buyer-context photography (catering buffet service)"
    - path: "dona-pattal-webApp/src/components/ui/EditorialImage.tsx"
      provides: "Image primitive with static aspect-ratio classes and Stitch-matching caption/badge overlays"
      contains: "aspectClass"
  key_links:
    - from: "dona-pattal-webApp/src/components/sections/HeroSection.tsx"
      to: "dona-pattal-webApp/public/images/hero/paper-plate-stack.png"
      via: "EditorialImage src prop"
      pattern: "src=\"/images/"
    - from: "dona-pattal-webApp/src/content/products.ts"
      to: "dona-pattal-webApp/public/images/products/"
      via: "Product.image field consumed by ProductCard next/image"
      pattern: "/images/products/product-(dona|plate)\\.jpg"
---

<objective>
Put real, locally hosted photography behind every `<Image>` in the app, fix the `EditorialImage` sizing bug that silently discards aspect ratios, and drop the now-unused remote image host from `next.config.ts`.

Purpose: Pillar 2 of `docs/UI-REVIEW.md` scored 1/4 because every photographic image on every route is broken — Next's optimizer returns HTTP 500 for the ephemeral `lh3.googleusercontent.com/aida*` Stitch mockup URLs, and `public/` holds zero image files. The site's entire "Editorial Natural / large photography as storytelling" identity is absent. This plan restores it (IMG-01, IMG-02).

Output: Six vetted photographs committed under `dona-pattal-webApp/public/images/`, all 11 hard-coded remote `src` values repointed to local paths, `EditorialImage` corrected, and `next.config.ts` `images.remotePatterns` emptied.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@docs/UI-REVIEW.md
@stitch-extracted/homepage/DESIGN.md
</context>

<asset_inventory>
Verified by inspecting every image file in the repository (excluding `node_modules`/`.git`). `dona-pattal-webApp/public/` is empty; the only real photographs in the repo live in `html-design/v2/assets/images/`.

**USABLE — real photographs, no baked-in text, on-brand:**

| Source file | Dimensions | Subject |
|---|---|---|
| `html-design/v2/assets/images/about-paper-pattal.png` | 1280x705 RGBA (transparent cutout) | Tall stack of printed paper plates |
| `html-design/v2/assets/images/about-dona-pattal.jpg` | 1280x961 | Traditional sal-leaf dona and pattal stacks on a table |
| `html-design/v2/assets/images/hero-food.jpg` | 1200x800 | Caterer in gloves serving from a buffet line |
| `html-design/v2/assets/images/about-sweets.jpg` | 1200x1600 | Indian sweet-shop display trays (mithai) |
| `html-design/v2/assets/images/product-dona.jpg` | 800x1199 | Stack of kraft paper cups on white |
| `html-design/v2/assets/images/product-plate.jpg` | 800x1200 | Red and white disposable paper plates with napkins |

**REJECTED — do NOT use as photographic content:** `hero-cover-bg.png`, `new_image.png`, `finlal_bg.png`, and the six `ChatGPT Image Sep 16, 2026, *.png` files. All are AI-generated marketing banners with baked-in headline text, logos and badge graphics; `hero-cover-bg.png` additionally renders a human likeness resembling a well-known actor. They are posters, not editorial photography, and are unsuitable as `<Image>` content under the "Editorial Natural, NOT a Shopify template" direction in `stitch-extracted/homepage/DESIGN.md`.

**NOT AVAILABLE:** There is no photograph of the Mirzapur factory, production line, or manufacturing equipment anywhere in the repository. Three slots currently claim to show one. Task 1 resolves that.
</asset_inventory>

<design_precedence_note>
`stitch-extracted/*/code.html` and `stitch-extracted/*/DESIGN.md` disagree in places. Precedence for this phase:

1. `REQUIREMENTS.md` wins over both.
2. `DESIGN.md` wins over `code.html` on the colour system (all five `DESIGN.md` files are byte-identical and declare exactly two accents: brick + brass).
3. `code.html` wins over `DESIGN.md` on layout, spacing and type scale (it is the markup the app was ported from and it matches the implementation closely).

Concretely: `code.html` itself uses `bg-emerald-700` for WhatsApp CTAs. That is **not** a target to preserve — COLOR-01 requires removing it. Do not "restore" green while matching `code.html`.
</design_precedence_note>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide how to handle the three factory-photography slots</name>

  <read_first>
    - `docs/UI-REVIEW.md` (Pillar 2 section, lines 67-74)
    - `.planning/PROJECT.md` (Constraints section — "No fabricated assets")
    - `dona-pattal-webApp/src/components/sections/MaterialMakingSection.tsx` (lines 38-52)
    - `dona-pattal-webApp/src/components/sections/MirzapurSection.tsx` (lines 8-24)
    - `dona-pattal-webApp/src/components/sections/products/FactoryDirectBanner.tsx` (lines 22-30)
    - The `<asset_inventory>` block above
  </read_first>

  <decision>
    Three image slots currently assert, in `alt` text and captions, that they depict the Mirzapur manufacturing facility. No such photograph exists in the repository, and `.planning/PROJECT.md` forbids substituting invented assets presented as real.

    Affected slots:
    - `MaterialMakingSection.tsx:41-43` — alt "Sahlok Eco Products manufacturing workshop still photo in Mirzapur", overlaid with a play button and the heading "See How We Make It" (there is no video behind it either).
    - `MirzapurSection.tsx:11-13` — alt "Sahlok Eco Products manufacturing workshop in Mirzapur Uttar Pradesh", caption block "Mirzapur Facility / Civil Line Road, Saripur Jalalpur".
    - `FactoryDirectBanner.tsx:24-27` — alt "Sahlok Eco Products factory interior with paper manufacturing equipment", caption "MIRZAPUR MANUFACTURING UNIT".
  </decision>

  <context>
    This is a B2B supplier site; a fabricated or misleading factory photo is a credibility and honesty problem, not a cosmetic one. The business owner may simply have real photos that were never added to the repo, which would be the best outcome by far.
  </context>

  <options>
    <option id="supply-real">
      <name>Supply real factory photos</name>
      <pros>Highest credibility; fully satisfies IMG-01 as originally intended; "See How We Make It" becomes truthful.</pros>
      <cons>Requires the user to drop image files into `dona-pattal-webApp/public/images/factory/` before this plan can finish.</cons>
    </option>
    <option id="honest-substitute">
      <name>Use real product/material photography with corrected, non-claiming copy (DEFAULT if no photos are supplied)</name>
      <pros>Ships now with only real photographs; no fabrication. Nothing claims to show a factory.</pros>
      <cons>Sections lose their manufacturing-process visual; copy must be rewritten so it no longer promises factory footage (including removing the play-button affordance on "See How We Make It").</cons>
    </option>
    <option id="remove-slots">
      <name>Remove the three image slots and re-lay those sections as text/spec treatments</name>
      <pros>No misleading imagery at all; smallest asset surface.</pros>
      <cons>Largest deviation from `stitch-extracted/homepage/code.html` layout; these are three of the largest visual blocks on the site.</cons>
    </option>
  </options>

  <acceptance_criteria>
    - A written decision is recorded in the plan SUMMARY naming exactly one option id: `supply-real`, `honest-substitute`, or `remove-slots`.
    - If `supply-real`: the supplied files exist on disk under `dona-pattal-webApp/public/images/factory/` and `ls dona-pattal-webApp/public/images/factory/` lists at least one file before Task 3 begins.
    - If `honest-substitute` or `remove-slots`: the SUMMARY records "real Mirzapur factory photography still required" as an open follow-up item.
  </acceptance_criteria>

  <resume-signal>Reply with one of: `supply-real` (and place the files first), `honest-substitute`, or `remove-slots`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit vetted photography into public/images and fix EditorialImage</name>

  <read_first>
    - `dona-pattal-webApp/src/components/ui/EditorialImage.tsx` (all 53 lines)
    - `dona-pattal-webApp/src/components/ui/EditorialImage.test.tsx`
    - `dona-pattal-webApp/node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md` (`fill`, `sizes`, `object-fit` sections)
    - `stitch-extracted/homepage/code.html` lines 158-172 (the hero image container the app was ported from)
    - The `<asset_inventory>` and `<design_precedence_note>` blocks above
  </read_first>

  <action>
    **2a. Copy the six usable source files** from `html-design/v2/assets/images/` into `dona-pattal-webApp/public/` at exactly these destinations (copy, do not move — `html-design/` stays intact as the historical design reference):

    | Source (`html-design/v2/assets/images/`) | Destination (`dona-pattal-webApp/public/`) |
    |---|---|
    | `about-paper-pattal.png` | `images/hero/paper-plate-stack.png` |
    | `about-dona-pattal.jpg` | `images/editorial/leaf-dona-pattal.jpg` |
    | `hero-food.jpg` | `images/editorial/catering-service.jpg` |
    | `about-sweets.jpg` | `images/editorial/sweet-shop-trays.jpg` |
    | `product-dona.jpg` | `images/products/product-dona.jpg` |
    | `product-plate.jpg` | `images/products/product-plate.jpg` |

    The two `images/products/` destinations are the paths `src/content/products.ts` already points at (lines 31, 45, 59, 73, 87, 101) — copying the files is the entire fix for the six product cards; do not edit `products.ts` in this plan.

    Copy none of the rejected AI banner files.

    **2b. Fix the aspect-ratio bug in `EditorialImage.tsx`.** Line 32 currently builds the class with a template string: `` `relative aspect-[${aspectRatio}] overflow-hidden ...` ``. Tailwind scans source text statically and never emits a rule for an interpolated class name, so every `aspectRatio` prop the app passes (`16/10`, `16/9`, `4/3`) is silently dropped and the container collapses to whatever `bg-paper`/`border` gives it. Replace the interpolation with an explicit lookup keyed by the prop, in the same style as the existing `badgePositions` map, so every class string appears verbatim in the file:

    - `'16/10'` -> `aspect-[16/10]`
    - `'16/9'` -> `aspect-[16/9]`
    - `'4/3'` -> `aspect-[4/3]`
    - `'4/5'` -> `aspect-[4/5]`
    - `'1/1'` -> `aspect-square`

    Keep `aspectRatio` defaulting to `'4/3'`, and fall back to the `4/3` class for any unlisted value. Narrow the prop type to that union of string literals so an unsupported ratio is a compile error rather than a silent layout bug. Then update the three call sites' prop values only if TypeScript rejects them (they currently pass `16/10`, `16/9`, `4/3`, all of which are in the map).

    **2c. Align the caption and badge overlays to `stitch-extracted/homepage/code.html`.** The reference markup (lines 160-170) is:
    - caption pill: `absolute bottom-5 left-5 bg-white/95 backdrop-blur-md px-4 py-2.5 rounded-sm border border-stone shadow-md flex items-center gap-2.5`, containing a `w-2.5 h-2.5 rounded-full bg-brick` dot followed by `text-[11px] font-bold tracking-widest uppercase text-ink` label text.
    - badge: `absolute top-5 right-5 bg-ink/90 text-paper px-3 py-1.5 rounded-sm text-[11px] font-semibold tracking-wider uppercase backdrop-blur-sm`.
    - outer container gains `shadow-xl`.

    The current implementation inverts both (caption is a full-bleed `bottom-4 left-4 right-4 bg-ink/90 text-paper` bar; badge is `bg-white/90 text-ink`). Swap them to match the reference above. Keep `badgePosition` working for all three existing values by applying the `top-5 right-5` / `top-5 left-5` / `bottom-5 left-5` offsets through the existing `badgePositions` map rather than hard-coding one corner.

    **2d. Update `EditorialImage.test.tsx`** so it covers the new behaviour: assert the rendered container carries the literal class `aspect-[16/9]` when `aspectRatio="16/9"` is passed, and that a rendered `caption` produces an element with the `text-ink` class (not `text-paper`).
  </action>

  <verify>
    <automated>cd dona-pattal-webApp && npx vitest run src/components/ui/EditorialImage.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `ls dona-pattal-webApp/public/images/hero/paper-plate-stack.png dona-pattal-webApp/public/images/products/product-dona.jpg dona-pattal-webApp/public/images/products/product-plate.jpg dona-pattal-webApp/public/images/editorial/leaf-dona-pattal.jpg dona-pattal-webApp/public/images/editorial/catering-service.jpg dona-pattal-webApp/public/images/editorial/sweet-shop-trays.jpg` exits 0.
    - `grep -c 'aspect-\[16/10\]\|aspect-\[16/9\]\|aspect-\[4/3\]' dona-pattal-webApp/src/components/ui/EditorialImage.tsx` returns 3 or more.
    - `grep -c 'aspect-\[\${' dona-pattal-webApp/src/components/ui/EditorialImage.tsx` returns 0.
    - `grep -c 'bg-white/95' dona-pattal-webApp/src/components/ui/EditorialImage.tsx` returns 1 or more (caption pill matches the Stitch reference).
    - `cd dona-pattal-webApp && npx vitest run src/components/ui/EditorialImage.test.tsx` exits 0.
    - No file matching `ChatGPT Image*`, `hero-cover-bg.png`, `new_image.png` or `finlal_bg.png` exists anywhere under `dona-pattal-webApp/public/`.
  </acceptance_criteria>

  <done>All six vetted photographs are on disk under `public/images/`, `EditorialImage` emits static aspect classes and Stitch-matching overlays, and its unit test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint every remote image src to a local asset and empty remotePatterns</name>

  <read_first>
    - `dona-pattal-webApp/next.config.ts`
    - `dona-pattal-webApp/node_modules/next/dist/docs/01-app/03-api-reference/05-config/01-next-config-js/images.md` (`remotePatterns` section)
    - Each file in the mapping table below, at the cited line
    - The Task 1 decision recorded in the SUMMARY
    - The `<escape_map>` and `<color_map>` blocks below
  </read_first>

  <action>
    **3a. Replace all 11 `lh3.googleusercontent.com` `src` values.** Confirm the current set first with `grep -rn "lh3.googleusercontent.com" dona-pattal-webApp/src` (expect exactly 11 hits). Apply this mapping:

    | File | Line | New `src` | `alt` |
    |---|---|---|---|
    | `src/components/sections/HeroSection.tsx` | 47 | `/images/hero/paper-plate-stack.png` | keep existing (already accurate) |
    | `src/components/sections/about/AboutHero.tsx` | 40 | `/images/editorial/leaf-dona-pattal.jpg` | rewrite to describe traditional sal-leaf dona and pattal stacks |
    | `src/components/sections/about/ProductFocusSection.tsx` | 22 | `/images/products/product-dona.jpg` | rewrite to describe stacked kraft paper dona/cups |
    | `src/components/sections/about/ProductFocusSection.tsx` | 45 | `/images/products/product-plate.jpg` | rewrite to describe disposable paper plates with napkins |
    | `src/components/sections/about/ProductFocusSection.tsx` | 68 | `/images/hero/paper-plate-stack.png` | rewrite to describe a tall stack of printed paper plates |
    | `src/components/sections/contact/ContactHero.tsx` | 41 | `/images/editorial/catering-service.jpg` | rewrite to describe a caterer serving from a buffet line |
    | `src/app/wholesale/page.tsx` | 52 | `/images/editorial/catering-service.jpg` | rewrite to describe bulk catering service |
    | `src/app/wholesale/page.tsx` | 126 | `/images/products/product-plate.jpg` | rewrite to describe paper plates stacked for bulk dispatch |
    | `src/components/sections/MaterialMakingSection.tsx` | 41 | per Task 1 decision | per Task 1 decision |
    | `src/components/sections/MirzapurSection.tsx` | 11 | per Task 1 decision | per Task 1 decision |
    | `src/components/sections/products/FactoryDirectBanner.tsx` | 24 | per Task 1 decision | per Task 1 decision |

    For the three Task 1 slots: if the decision was `honest-substitute`, use `/images/editorial/leaf-dona-pattal.jpg` for `MaterialMakingSection` and `MirzapurSection` and `/images/hero/paper-plate-stack.png` for `FactoryDirectBanner`, and rewrite every `alt`, `caption` and heading in those three blocks so none of them states or implies that the photo shows a factory, workshop, production line or manufacturing equipment. In `MaterialMakingSection` also remove the play-button circle (`<div className="w-16 h-16 rounded-full bg-brick ...">` with the triangle `<svg>`, around lines 46-48) and change the "See How We Make It" heading, because there is no video behind it. If the decision was `supply-real`, point each slot at the supplied file under `/images/factory/` and leave the existing copy. If it was `remove-slots`, delete the `EditorialImage` element and its wrapper from each of the three files and keep the surrounding text content.

    Every `src` value must be a root-relative path beginning with `/images/`. Do not use a remote URL and do not import the file as a static import — the surrounding code passes `src` as a string prop to `EditorialImage`.

    **3b. Empty `images.remotePatterns` in `next.config.ts`.** After 3a, `grep -rn "lh3.googleusercontent.com" dona-pattal-webApp/src` must return nothing, and no other remote host is used anywhere in the app. Remove the `lh3.googleusercontent.com` entry. Because `remotePatterns` is then an empty allow-list, delete the whole `images` key rather than leaving `images: { remotePatterns: [] }`; the resulting config should be the bare `NextConfig` object. Leaving a wildcard `pathname: '/**'` entry for a host the app no longer loads from is an unnecessary SSRF-adjacent surface on the optimizer endpoint (see `<threat_model>`).

    **3c. Clean up the remaining defects in the files this plan owns.** These files are being edited anyway; leaving known-garbage output in them is not acceptable.

    In `src/app/wholesale/page.tsx`, `src/components/sections/HeroSection.tsx`, `src/components/sections/MaterialMakingSection.tsx`, `src/components/sections/MirzapurSection.tsx`, `src/components/sections/products/FactoryDirectBanner.tsx`, `src/components/sections/about/ProductFocusSection.tsx` and `src/components/sections/contact/ContactHero.tsx`:

    - Replace every literal `\uXXXX` sequence with the real character per `<escape_map>` below (TXT-01, TXT-02).
    - Replace every `emerald`/`green-*` Tailwind class per `<color_map>` below (COLOR-01).

    Do not touch any file outside this plan's `files_modified` list — plans 01-02 and 01-03 own the rest and run in the same wave.
  </action>

  <verify>
    <automated>cd dona-pattal-webApp && npm run lint && npm run test && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "lh3.googleusercontent.com" dona-pattal-webApp/` (excluding `node_modules`, `.git`, `stitch-extracted`, `.planning`) returns no matches.
    - `grep -c 'remotePatterns\|googleusercontent' dona-pattal-webApp/next.config.ts` returns 0.
    - `grep -rno 'src="[^"]*"' dona-pattal-webApp/src --include=*.tsx | grep -v 'src="/images/' | grep -c 'https\?://'` returns 0.
    - `grep -rn '\\u[0-9a-fA-F]\{4\}' dona-pattal-webApp/src/app/wholesale/page.tsx dona-pattal-webApp/src/components/sections/HeroSection.tsx dona-pattal-webApp/src/components/sections/MaterialMakingSection.tsx dona-pattal-webApp/src/components/sections/MirzapurSection.tsx dona-pattal-webApp/src/components/sections/products/FactoryDirectBanner.tsx dona-pattal-webApp/src/components/sections/about/ProductFocusSection.tsx dona-pattal-webApp/src/components/sections/contact/ContactHero.tsx` returns no matches.
    - `grep -rn 'emerald\|green-' dona-pattal-webApp/src/app/wholesale/page.tsx dona-pattal-webApp/src/components/sections/MaterialMakingSection.tsx` returns no matches.
    - `cd dona-pattal-webApp && npm run lint` exits 0.
    - `cd dona-pattal-webApp && npm run test` exits 0.
    - `cd dona-pattal-webApp && npm run build` exits 0.
    - With `npm run dev` running, `curl -s -o /dev/null -w "%{http_code}" "http://localhost:3000/_next/image?url=%2Fimages%2Fhero%2Fpaper-plate-stack.png&w=1200&q=75"` returns `200` (it returned `500` for the old remote URLs).
    - No `alt` or caption string in `MaterialMakingSection.tsx`, `MirzapurSection.tsx` or `FactoryDirectBanner.tsx` contains `factory`, `workshop`, `manufacturing unit` or `production line` unless the Task 1 decision was `supply-real`.
  </acceptance_criteria>

  <done>All routes load real local photography through the Next image optimizer with HTTP 200, `next.config.ts` allows no remote image hosts, and lint/test/build pass.</done>
</task>

</tasks>

<escape_map>
Replace each literal escape sequence with the character shown. Type the real character directly into the source file (the files are UTF-8).

| Literal in source | Character | Name |
|---|---|---|
| `\u00a9` | © | copyright sign |
| `\u00b0` | ° | degree sign |
| `\u00b7` | · | middle dot |
| `\u2013` | – | en dash |
| `\u2014` | — | em dash |
| `\u2019` | ’ | right single quotation mark |
| `\u201C` | “ | left double quotation mark |
| `\u201D` | ” | right double quotation mark |
| `\u2022` | • | bullet |
| `\u2192` | → | rightwards arrow |
| `\u2209` | — | (not present; ignore) |
| `\u223c` | ∼ | tilde operator |
| `\u23F1` | ⏱ | stopwatch |
| `\u23F1\uFE0F` | ⏱️ | stopwatch + variation selector (replace the pair together) |
| `\u260e` | ☎ | black telephone |
| `\u267B\uFE0F` | ♻️ | recycling symbol + variation selector (replace the pair together) |
| `\u2709` | ✉ | envelope |
| `\u2713` | ✓ | check mark |
| `\uD83C\uDF74` | 🍴 | fork and knife (surrogate pair — replace both halves as one) |
| `\uD83D\uDCCD` | 📍 | round pushpin (surrogate pair) |
| `\uD83D\uDCCF` | 📏 | straight ruler (surrogate pair) |
| `\uD83D\uDCE6` | 📦 | package (surrogate pair) |
| `\uD83D\uDE9A` | 🚚 | delivery truck (surrogate pair) |

Why these render as garbage today: JSX text nodes and JSX attribute strings are not JavaScript string literals. A backslash in `<span>\u2192</span>` or in `label="Ask for this size \u2192"` is emitted verbatim, so visitors read the six characters `\u2192`. The same sequence inside a real JS string or template literal (`{'\u2192'}`, `` {`${a} \u00b7 ${b}`} ``, or anything in a `.ts` file) *is* decoded correctly and is not currently broken — but replacing it with the literal character there is behaviour-preserving and makes a single repo-wide grep gate possible. Never convert a working escape into a double-escaped one (`\\u2192`).
</escape_map>

<color_map>
COLOR-01: no `emerald`/`green-*` Tailwind class may survive anywhere in `dona-pattal-webApp/src`. Targets are the tokens already declared in `src/app/globals.css` (`--color-brick #B24A2E`, `--color-brass #A87C3F`, `--color-oxblood #4A1F1C`, `--color-ink #2B2521`, `--color-paper #F4EFE6`). Do not add a new green token.

| Current | Replacement | Applies to |
|---|---|---|
| `bg-emerald-700 hover:bg-emerald-800 text-white` | `bg-oxblood hover:bg-ink text-paper` | WhatsApp CTA buttons |
| `text-emerald-700` on a checkmark glyph | `text-brass` | `wholesale/page.tsx:116` (matches the sibling checkmarks on lines 26/31/36/85) |
| `text-emerald-700` on a link | `text-brick` | inline text links |
| `text-emerald-800` | `text-oxblood` | inline text links |
| `hover:text-emerald-700` | `hover:text-brick` | link hover |
| `text-emerald-400` on a dark `bg-ink` surface | `text-brass` | `MaterialMakingSection.tsx:65` |

Rationale for oxblood rather than brick on the WhatsApp button: `DESIGN.md` reserves brick for the primary CTA, and `HeroSection.tsx` already renders `<Button variant="primary">GET BULK PRICING</Button>` immediately beside the WhatsApp button — making both brick would erase the distinction. Oxblood is the declared dark-surface token, keeps the pair legible, and stays inside the two-accent system.
</color_map>

<artifacts_this_phase_produces>
Created by this plan:

| Artifact | Kind | Path |
|---|---|---|
| `paper-plate-stack.png` | static asset | `dona-pattal-webApp/public/images/hero/paper-plate-stack.png` |
| `product-dona.jpg` | static asset | `dona-pattal-webApp/public/images/products/product-dona.jpg` |
| `product-plate.jpg` | static asset | `dona-pattal-webApp/public/images/products/product-plate.jpg` |
| `leaf-dona-pattal.jpg` | static asset | `dona-pattal-webApp/public/images/editorial/leaf-dona-pattal.jpg` |
| `catering-service.jpg` | static asset | `dona-pattal-webApp/public/images/editorial/catering-service.jpg` |
| `sweet-shop-trays.jpg` | static asset | `dona-pattal-webApp/public/images/editorial/sweet-shop-trays.jpg` |
| `aspectClass` | const lookup map in `EditorialImage.tsx` | `dona-pattal-webApp/src/components/ui/EditorialImage.tsx` |
| `AspectRatio` | exported TS union type (`'16/10' \| '16/9' \| '4/3' \| '4/5' \| '1/1'`) | `dona-pattal-webApp/src/components/ui/EditorialImage.tsx` |

Conditionally created (only if Task 1 resolves to `supply-real`): `dona-pattal-webApp/public/images/factory/` and its contents.

Removed by this plan: the `images` key (and its `remotePatterns` array) in `dona-pattal-webApp/next.config.ts`.

No new components, routes, exported functions, CLI flags or environment variables are introduced.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Visitor -> `/_next/image` optimizer | The only endpoint in this phase that takes attacker-influenced input (`url`, `w`, `q` query params) and performs a fetch on the server's behalf. |
| Repository -> `public/` | Files placed here are served verbatim to every visitor with no authentication or transformation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Information Disclosure / SSRF | `next.config.ts` `images.remotePatterns` | mitigate | Task 3b deletes the `images` key entirely rather than adding hosts. The existing entry allowed `https://lh3.googleusercontent.com/**`; with it removed the optimizer accepts only root-relative `/images/...` paths, so no attacker-supplied `url` can make the server fetch an external origin. This plan adds **no** new `remotePatterns` entry — if a future task needs one it must pin `hostname`, `protocol: 'https'` and a specific `pathname` prefix, never `/**` on a shared user-content CDN. |
| T-01-02 | Tampering | Image assets copied into `public/` | mitigate | All six files already exist inside this repository under `html-design/v2/assets/` and are copied, not downloaded. No network fetch, package install or third-party CDN is introduced, so there is no new supply-chain step to audit. |
| T-01-03 | Spoofing / misrepresentation | `alt` text and captions on the three factory slots | mitigate | Task 1 is a blocking decision checkpoint and Task 3 has an explicit acceptance criterion forbidding the words `factory`, `workshop`, `manufacturing unit` and `production line` unless a genuine photo was supplied. Publishing a stock or AI-generated image captioned as this company's own facility would be a material misrepresentation to B2B buyers. |
| T-01-04 | Information Disclosure / rights | Rejected AI banner assets | mitigate | `hero-cover-bg.png` and the `ChatGPT Image *.png` set are excluded by name in `<asset_inventory>` and by acceptance criterion in Task 2. `hero-cover-bg.png` depicts a likeness resembling a well-known actor; publishing it would create a personality-rights exposure. |
| T-01-05 | Elevation of Privilege | n/a | accept | This plan changes no authentication, authorization, form handling, API route or server action. `EnquiryForm`/`ContactForm` input handling is untouched — confirmed: no file in `files_modified` reads request input, cookies, headers or environment variables. No new user-input surface is created. |

No package-manager install occurs in this plan, so the Package Legitimacy Gate does not apply and no `T-01-SC` checkpoint is required. `npm run build` is run against the already-installed, already-committed dependency tree.
</threat_model>

<verification>
1. `cd dona-pattal-webApp && npm run lint && npm run test && npm run build` — all exit 0.
2. `grep -rn "lh3.googleusercontent.com" dona-pattal-webApp/src` — no matches.
3. `grep -rn "remotePatterns" dona-pattal-webApp/next.config.ts` — no matches.
4. `npm run dev`, then for each of `/`, `/about`, `/contact`, `/wholesale`, `/products`, `/products/8-inch-plate`: every `<img>` has a non-zero `naturalWidth` and no bordered empty box is visible.
5. `curl -s -o /dev/null -w "%{http_code}" "http://localhost:3000/_next/image?url=%2Fimages%2Fproducts%2Fproduct-plate.jpg&w=640&q=75"` returns `200`.
</verification>

<success_criteria>
- All 11 previously remote `src` values resolve to files under `dona-pattal-webApp/public/images/` and return HTTP 200 through `/_next/image`.
- The six product cards render photography (fixed by the file copy alone; `src/content/products.ts` is untouched).
- `EditorialImage` emits statically analysable `aspect-*` classes and Stitch-matching caption/badge overlays.
- `next.config.ts` declares no remote image host.
- Zero `\uXXXX` literals and zero `emerald`/`green-*` classes remain in the nine source files this plan owns.
- `npm run lint`, `npm run test` and `npm run build` all pass.
</success_criteria>

<output>
Create `.planning/phases/01-fix-images-encoding-brand-color-align-to-stitch-designs/01-01-SUMMARY.md` when done. Record the Task 1 decision verbatim, the final slot-to-asset mapping, and any follow-up item (for example "real Mirzapur factory photography still required").
</output>
